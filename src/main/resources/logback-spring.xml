<?xml version="1.0" encoding="UTF-8"?>
<configuration>
 
    <property name="LOG_DIR" value="./logs" />
    <property name="LOG_DIR_ARCHIVE" value="./logs/archived" />
 
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>
                %black(%d{ISO8601}) %highlight(%-5level) [%blue(%t)] %blue(%C{1.}): %msg%n%throwable
            </Pattern>
        </layout>
    </appender>
    
<!--    <appender name="File-Thread" class="ch.qos.logback.classic.sift.SiftingAppender">

         This is MDC value 
         We will assign a value to 'logFileName' via Java code 
        <discriminator>
            <key>logFileName</key>
            <defaultValue>head0</defaultValue>
        </discriminator>

        <sift>
             A standard RollingFileAppender, the log file is based on 'logFileName' at runtime  
            <appender name="FILE-${logFileName}" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <file>${LOG_DIR}/${logFileName}.log</file>

                <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                    <Pattern>
                        %d{yyyy-MM-dd HH:mm:ss} %mdc [%thread] %level %logger{35} - %msg%n
                    </Pattern>
                </encoder>

                <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">                                         
                    <fileNamePattern>${LOG_DIR_ARCHIVE}/${logFileName}.%d{yyyy-MM-dd HH:mm:ss}.log.gz</fileNamePattern>
                    <maxHistory>30</maxHistory>
                </rollingPolicy>
            </appender>
        </sift>
    </appender>-->

    <root level="info">
        <appender-ref ref="Console" />
    </root>
    
<!--    <logger name="it.bologna.ausl.shpeck.service.worker" level="debug" additivity="false">
        <appender-ref ref="File-Thread" />
        <appender-ref ref="Console" />
    </logger>    -->
 
</configuration>